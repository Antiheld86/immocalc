services:
  immocalc:
    build: .
    image: immocalc:2.1
    container_name: immocalc
    restart: unless-stopped
    ports:
      - "8087:80"
    # Reverse Proxy statt Portfreigabe? Dann ports auskommentieren
    # und den Container ins Proxy-Netz hängen:
    # networks:
    #   - proxy
    # labels:
    #   - "traefik.enable=true"
    #   - "traefik.http.routers.immocalc.rule=Host(`immo.deine-domain.de`)"
    #   - "traefik.http.services.immocalc.loadbalancer.server.port=80"

# networks:
#   proxy:
#     external: true
